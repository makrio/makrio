<div id="framer-wrapper">
    <h2 style="margin:20px 0;">
        {{#if sourcePost}}
            Remixing {{author.name}}'s <a href="/p/{{sourcePost.id}}" target="_blank">Post</a>
        {{else}}
           Add Photos & Text
        {{/if}}
    </h2>

    <div class="flow-content"/>

    <div class="flow-controls">
        <div class="controls"/>
    </div>
</div>